services:
  postgres:
    container_name: nest-kukac-postgres
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: nest-kukac
    volumes:
      - ./data/pg:/var/lib/postgresql/data
